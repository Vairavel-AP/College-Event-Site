apiVersion: v1
kind: Service

metadata:
  name: college-event-site

spec:
  type: NodePort

  selector:
    app: college-event-site

  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
